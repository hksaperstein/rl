gymnasium==1.3.0
matplotlib==3.11.1
numpy==2.5.1
pillow==12.3.0
pytest==9.1.1
stable_baselines3==2.9.0
# torch installed CPU-only, from the dedicated index below (NOT plain PyPI --
# plain PyPI's aarch64 torch wheel pulls in ~1.5GB of unused NVIDIA
# cuda-toolkit/cudnn packages on this GPU-less Raspberry Pi; the Pi has no
# NVIDIA GPU at all, so those packages are pure waste here).
# Install via:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
torch==2.13.0+cpu
