# Detector training (install torch/torchvision from the cu128 index — see docs/superpowers/plans/2026-07-10-dice-detector-v1.md Task 1)
torch>=2.7
torchvision
ultralytics
roboflow
pycocotools
pytest
numpy
opencv-python
matplotlib
pyyaml
# ONNX export (scripts/export_model.py); ultralytics auto-installs these on
# first `model.export(format="onnx")` call if missing -- pinned here too so
# a fresh venv install doesn't depend on that first-run network fetch.
onnx>=1.12.0,<2.0.0
onnxruntime
onnxslim>=0.1.82
# Plain pxr/USD inspection with no Isaac Sim/Kit dependency (Isaac's own pxr
# bindings require the full Kit environment via isaacsim.SimulationApp --
# see scripts/_diag_dice_material_check.py's docstring). Used to fresh-reload
# and verify dice USD exports (structure/material/geometry) directly from
# vision/.venv -- see .superpowers/sdd/task-dice-usd-materials-report.md.
usd-core
